# Paysecure Retry Demo
A Node.js prototype to retry failed API calls (e.g., payment timeouts) with exponential backoff. Built to explore boosting approval rates for Paysecure.

## How to Run
1. Clone this repo.
2. Run `npm install`.
3. Run `node retry.js`.
